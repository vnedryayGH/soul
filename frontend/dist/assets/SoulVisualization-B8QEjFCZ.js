import { a as reactExports, j as jsxRuntimeExports } from "./react-DAIzMmXQ.js";
import { C as Card, T as Tabs } from "./index-C8B9-ZwJ.js";
import { cC as SearchOutlined$1, l as Space, aB as Tooltip, B as Button, n as Select, T as Typography, I as Input, aC as Spin, c as apiRequest, s as staticMethods } from "./index-B4P9h-k1.js";
import { I as Icon } from "./AntdIcon-bc3Msg1y.js";
import { R as RefIcon$5 } from "./ReloadOutlined-b-zgDpPK.js";
import { a as Slider } from "./index-BXtDgZ6P.js";
import { S as Switch } from "./index-C97PeQQx.js";
import { R as RefIcon$6, a as RefIcon$9 } from "./ShareAltOutlined-CzSqXZex.js";
import { R as Row, C as Col } from "./row-BcQp44VL.js";
import { S as Statistic } from "./index-8wmSld-G.js";
import { R as RefIcon$7 } from "./ApiOutlined-Bc6k0MAE.js";
import { R as RefIcon$8 } from "./ThunderboltOutlined-DKtcPo4_.js";
import { A as Alert } from "./index-DVLFW87y.js";
import "./Skeleton-D3e3aC7P.js";
import "./index-BlJydARW.js";
var ZoomInOutlined$1 = { "icon": { "tag": "svg", "attrs": { "viewBox": "64 64 896 896", "focusable": "false" }, "children": [{ "tag": "path", "attrs": { "d": "M637 443H519V309c0-4.4-3.6-8-8-8h-60c-4.4 0-8 3.6-8 8v134H325c-4.4 0-8 3.6-8 8v60c0 4.4 3.6 8 8 8h118v134c0 4.4 3.6 8 8 8h60c4.4 0 8-3.6 8-8V519h118c4.4 0 8-3.6 8-8v-60c0-4.4-3.6-8-8-8zm284 424L775 721c122.1-148.9 113.6-369.5-26-509-148-148.1-388.4-148.1-537 0-148.1 148.6-148.1 389 0 537 139.5 139.6 360.1 148.1 509 26l146 146c3.2 2.8 8.3 2.8 11 0l43-43c2.8-2.7 2.8-7.8 0-11zM696 696c-118.8 118.7-311.2 118.7-430 0-118.7-118.8-118.7-311.2 0-430 118.8-118.7 311.2-118.7 430 0 118.7 118.8 118.7 311.2 0 430z" } }] }, "name": "zoom-in", "theme": "outlined" };
var ZoomOutOutlined$1 = { "icon": { "tag": "svg", "attrs": { "viewBox": "64 64 896 896", "focusable": "false" }, "children": [{ "tag": "path", "attrs": { "d": "M637 443H325c-4.4 0-8 3.6-8 8v60c0 4.4 3.6 8 8 8h312c4.4 0 8-3.6 8-8v-60c0-4.4-3.6-8-8-8zm284 424L775 721c122.1-148.9 113.6-369.5-26-509-148-148.1-388.4-148.1-537 0-148.1 148.6-148.1 389 0 537 139.5 139.6 360.1 148.1 509 26l146 146c3.2 2.8 8.3 2.8 11 0l43-43c2.8-2.7 2.8-7.8 0-11zM696 696c-118.8 118.7-311.2 118.7-430 0-118.7-118.8-118.7-311.2 0-430 118.8-118.7 311.2-118.7 430 0 118.7 118.8 118.7 311.2 0 430z" } }] }, "name": "zoom-out", "theme": "outlined" };
var DownloadOutlined$1 = { "icon": { "tag": "svg", "attrs": { "viewBox": "64 64 896 896", "focusable": "false" }, "children": [{ "tag": "path", "attrs": { "d": "M505.7 661a8 8 0 0012.6 0l112-141.7c4.1-5.2.4-12.9-6.3-12.9h-74.1V168c0-4.4-3.6-8-8-8h-60c-4.4 0-8 3.6-8 8v338.3H400c-6.7 0-10.4 7.7-6.3 12.9l112 141.8zM878 626h-60c-4.4 0-8 3.6-8 8v154H214V634c0-4.4-3.6-8-8-8h-60c-4.4 0-8 3.6-8 8v198c0 17.7 14.3 32 32 32h684c17.7 0 32-14.3 32-32V634c0-4.4-3.6-8-8-8z" } }] }, "name": "download", "theme": "outlined" };
var FilterOutlined$1 = { "icon": { "tag": "svg", "attrs": { "viewBox": "64 64 896 896", "focusable": "false" }, "children": [{ "tag": "path", "attrs": { "d": "M880.1 154H143.9c-24.5 0-39.8 26.7-27.5 48L349 597.4V838c0 17.7 14.2 32 31.8 32h262.4c17.6 0 31.8-14.3 31.8-32V597.4L907.7 202c12.2-21.3-3.1-48-27.6-48zM603.4 798H420.6V642h182.9v156zm9.6-236.6l-9.5 16.6h-183l-9.5-16.6L212.7 226h598.6L613 561.4z" } }] }, "name": "filter", "theme": "outlined" };
function _extends$4() {
  _extends$4 = Object.assign ? Object.assign.bind() : function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends$4.apply(this, arguments);
}
const DownloadOutlined = (props, ref) => /* @__PURE__ */ reactExports.createElement(Icon, _extends$4({}, props, {
  ref,
  icon: DownloadOutlined$1
}));
const RefIcon$4 = /* @__PURE__ */ reactExports.forwardRef(DownloadOutlined);
function _extends$3() {
  _extends$3 = Object.assign ? Object.assign.bind() : function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends$3.apply(this, arguments);
}
const FilterOutlined = (props, ref) => /* @__PURE__ */ reactExports.createElement(Icon, _extends$3({}, props, {
  ref,
  icon: FilterOutlined$1
}));
const RefIcon$3 = /* @__PURE__ */ reactExports.forwardRef(FilterOutlined);
function _extends$2() {
  _extends$2 = Object.assign ? Object.assign.bind() : function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends$2.apply(this, arguments);
}
const SearchOutlined = (props, ref) => /* @__PURE__ */ reactExports.createElement(Icon, _extends$2({}, props, {
  ref,
  icon: SearchOutlined$1
}));
const RefIcon$2 = /* @__PURE__ */ reactExports.forwardRef(SearchOutlined);
function _extends$1() {
  _extends$1 = Object.assign ? Object.assign.bind() : function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends$1.apply(this, arguments);
}
const ZoomInOutlined = (props, ref) => /* @__PURE__ */ reactExports.createElement(Icon, _extends$1({}, props, {
  ref,
  icon: ZoomInOutlined$1
}));
const RefIcon$1 = /* @__PURE__ */ reactExports.forwardRef(ZoomInOutlined);
function _extends() {
  _extends = Object.assign ? Object.assign.bind() : function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends.apply(this, arguments);
}
const ZoomOutOutlined = (props, ref) => /* @__PURE__ */ reactExports.createElement(Icon, _extends({}, props, {
  ref,
  icon: ZoomOutOutlined$1
}));
const RefIcon = /* @__PURE__ */ reactExports.forwardRef(ZoomOutOutlined);
const EnhancedGraphCanvas = reactExports.forwardRef(
  function EnhancedGraphCanvas2({
    data,
    width = 800,
    height = 600,
    onNodeClick,
    onEdgeClick,
    showHeatmap = true,
    showClusters = true,
    showTrends = false,
    enableFiltering = true
  }, ref) {
    const canvasRef = reactExports.useRef(null);
    const [dpr, setDpr] = reactExports.useState(1);
    const [pan, setPan] = reactExports.useState({ x: 0, y: 0 });
    const [scale, setScale] = reactExports.useState(1);
    const [layout, setLayout] = reactExports.useState({ nodes: {} });
    const [filteredData, setFilteredData] = reactExports.useState(null);
    const [selectedNode, setSelectedNode] = reactExports.useState(null);
    const [hoveredNode, setHoveredNode] = reactExports.useState(null);
    const [isDragging, setIsDragging] = reactExports.useState(false);
    const [dragStart, setDragStart] = reactExports.useState(null);
    const [filters, setFilters] = reactExports.useState({
      minEnergy: 0,
      maxEnergy: 1,
      selectedTags: [],
      selectedStates: [],
      timeRange: [0, 30]
      // последние 30 дней
    });
    const [visualSettings, setVisualSettings] = reactExports.useState({
      nodeSize: 1,
      edgeWidth: 1,
      showLabels: true,
      showEnergy: true,
      animateChanges: true,
      colorScheme: "energy"
    });
    reactExports.useEffect(() => {
      setDpr(typeof window !== "undefined" ? window.devicePixelRatio || 1 : 1);
    }, []);
    reactExports.useEffect(() => {
      if (!data) {
        setFilteredData(null);
        return;
      }
      const now = /* @__PURE__ */ new Date();
      const minDate = new Date(now.getTime() - filters.timeRange[1] * 24 * 60 * 60 * 1e3);
      const maxDate = new Date(now.getTime() - filters.timeRange[0] * 24 * 60 * 60 * 1e3);
      const filteredNodes = data.nodes.filter((node) => {
        const energy = node.energy_weight || 0;
        if (energy < filters.minEnergy || energy > filters.maxEnergy) return false;
        if (filters.selectedTags.length > 0) {
          const nodeTags = node.tags || [];
          if (!filters.selectedTags.some((tag) => nodeTags.includes(tag))) return false;
        }
        if (filters.selectedStates.length > 0) {
          if (!filters.selectedStates.includes(node.state || "unknown")) return false;
        }
        if (node.created_at) {
          const nodeDate = new Date(node.created_at);
          if (nodeDate < minDate || nodeDate > maxDate) return false;
        }
        return true;
      });
      const filteredNodeIds = new Set(filteredNodes.map((n) => n.id));
      const filteredEdges = data.edges.filter(
        (edge) => filteredNodeIds.has(edge.source) && filteredNodeIds.has(edge.target)
      );
      setFilteredData({
        nodes: filteredNodes,
        edges: filteredEdges
      });
    }, [data, filters]);
    reactExports.useEffect(() => {
      const gd = filteredData || { nodes: [], edges: [] };
      if (gd.nodes.length === 0) {
        setLayout({ nodes: {} });
        return;
      }
      const nodes = {};
      gd.nodes.forEach((n, i) => {
        const angle = i / gd.nodes.length * Math.PI * 2;
        const radius = Math.sqrt(gd.nodes.length) * 30;
        nodes[n.id] = {
          ...n,
          x: Math.cos(angle) * radius,
          y: Math.sin(angle) * radius
        };
      });
      const repulsion = 3e3;
      const attraction = 0.02;
      const damping = 0.9;
      const centeringForce = 1e-3;
      for (let iter = 0; iter < 150; iter++) {
        const keys = Object.keys(nodes);
        for (let i = 0; i < keys.length; i++) {
          for (let j = i + 1; j < keys.length; j++) {
            const a = nodes[keys[i]];
            const b = nodes[keys[j]];
            if (!a || !b) continue;
            const dx = a.x - b.x;
            const dy = a.y - b.y;
            const dist2 = Math.max(1, dx * dx + dy * dy);
            const dist = Math.sqrt(dist2);
            const energyFactor = ((a.energy_weight || 0.5) + (b.energy_weight || 0.5)) / 2;
            const force = repulsion * energyFactor / dist2;
            const fx = dx / dist * force;
            const fy = dy / dist * force;
            a.x += fx * damping;
            a.y += fy * damping;
            b.x -= fx * damping;
            b.y -= fy * damping;
          }
        }
        gd.edges.forEach((edge) => {
          const a = nodes[edge.source];
          const b = nodes[edge.target];
          if (!a || !b) return;
          const dx = b.x - a.x;
          const dy = b.y - a.y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist > 0) {
            const strength = edge.strength || 0.5;
            const force = attraction * strength * dist;
            const fx = dx / dist * force;
            const fy = dy / dist * force;
            a.x += fx;
            a.y += fy;
            b.x -= fx;
            b.y -= fy;
          }
        });
        keys.forEach((key) => {
          const node = nodes[key];
          node.x *= 1 - centeringForce;
          node.y *= 1 - centeringForce;
        });
      }
      setLayout({ nodes });
    }, [filteredData]);
    const draw = reactExports.useCallback(() => {
      const canvas = canvasRef.current;
      if (!canvas) return;
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      const W = width * dpr;
      const H = height * dpr;
      canvas.width = W;
      canvas.height = H;
      canvas.style.width = `${width}px`;
      canvas.style.height = `${height}px`;
      ctx.clearRect(0, 0, W, H);
      ctx.save();
      ctx.translate(W / 2 + pan.x * dpr, H / 2 + pan.y * dpr);
      ctx.scale(scale, scale);
      const nodes = layout.nodes;
      const edges = (filteredData == null ? void 0 : filteredData.edges) || [];
      if (showHeatmap) {
        Object.values(nodes).forEach((node) => {
          const energy = node.energy_weight || 0;
          if (energy <= 0) return;
          const radius = 60 + energy * 100;
          const gradient = ctx.createRadialGradient(node.x, node.y, 0, node.x, node.y, radius);
          const hue = Math.max(0, Math.min(360, energy * 60));
          gradient.addColorStop(0, `hsla(${hue}, 100%, 50%, 0.12)`);
          gradient.addColorStop(1, `hsla(${hue}, 100%, 50%, 0)`);
          ctx.fillStyle = gradient;
          ctx.beginPath();
          ctx.arc(node.x, node.y, radius, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (showTrends) {
        drawTrendLines(ctx, nodes);
      }
      edges.forEach((edge) => {
        const source = nodes[edge.source];
        const target = nodes[edge.target];
        if (!source || !target) return;
        const strength = edge.strength || 0.5;
        const alpha = Math.max(0.1, strength);
        ctx.strokeStyle = `rgba(100, 150, 200, ${alpha})`;
        ctx.lineWidth = (strength * 3 + 1) * visualSettings.edgeWidth * (dpr / scale);
        if (edge.connection_type === "semantic") {
          ctx.setLineDash([5, 5]);
        } else if (edge.connection_type === "temporal") {
          ctx.setLineDash([10, 2]);
        } else {
          ctx.setLineDash([]);
        }
        ctx.beginPath();
        ctx.moveTo(source.x, source.y);
        ctx.lineTo(target.x, target.y);
        ctx.stroke();
        if (strength > 0.7) {
          const midX = (source.x + target.x) / 2;
          const midY = (source.y + target.y) / 2;
          ctx.fillStyle = "rgba(255, 200, 0, 0.8)";
          ctx.beginPath();
          ctx.arc(midX, midY, 3, 0, Math.PI * 2);
          ctx.fill();
        }
      });
      if (showClusters) {
        drawClusters(ctx, nodes);
      }
      Object.values(nodes).forEach((node) => {
        var _a;
        const energy = node.energy_weight || 0.5;
        const baseRadius = 8 + energy * 12;
        const radius = baseRadius * visualSettings.nodeSize;
        const nodeColor = getNodeColor(node, visualSettings.colorScheme);
        if (selectedNode === node.id) {
          ctx.strokeStyle = "#ff4d4f";
          ctx.lineWidth = 4;
          ctx.beginPath();
          ctx.arc(node.x, node.y, radius + 4, 0, Math.PI * 2);
          ctx.stroke();
        } else if (hoveredNode === node.id) {
          ctx.strokeStyle = "#1890ff";
          ctx.lineWidth = 2;
          ctx.beginPath();
          ctx.arc(node.x, node.y, radius + 2, 0, Math.PI * 2);
          ctx.stroke();
        }
        ctx.fillStyle = nodeColor;
        ctx.strokeStyle = "rgba(255, 255, 255, 0.8)";
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.arc(node.x, node.y, radius, 0, Math.PI * 2);
        ctx.fill();
        ctx.stroke();
        if (visualSettings.showEnergy && energy > 0) {
          const ringRadius = radius + 3;
          const ringWidth = 2;
          ctx.strokeStyle = `rgba(0, 255, 0, ${energy})`;
          ctx.lineWidth = ringWidth;
          ctx.beginPath();
          ctx.arc(node.x, node.y, ringRadius, 0, Math.PI * 2 * energy);
          ctx.stroke();
        }
        if (node.delta_energy && Math.abs(node.delta_energy) > 0.01) {
          const deltaColor = node.delta_energy > 0 ? "#52c41a" : "#f5222d";
          const deltaSize = Math.abs(node.delta_energy) * 10;
          ctx.fillStyle = deltaColor;
          ctx.beginPath();
          ctx.arc(node.x + radius, node.y - radius, deltaSize, 0, Math.PI * 2);
          ctx.fill();
        }
        if (visualSettings.showLabels && scale > 0.5) {
          ctx.fillStyle = "rgba(0, 0, 0, 0.8)";
          ctx.font = `${12 / scale}px Arial`;
          ctx.textAlign = "center";
          ctx.textBaseline = "middle";
          const label = ((_a = node.thought_form) == null ? void 0 : _a.substring(0, 20)) || node.id.substring(0, 8);
          ctx.fillText(label, node.x, node.y + radius + 15);
        }
      });
      ctx.restore();
    }, [layout, filteredData, pan, scale, dpr, width, height, selectedNode, hoveredNode, showHeatmap, showClusters, showTrends, visualSettings]);
    reactExports.useEffect(() => {
      draw();
    }, [draw]);
    const getNodeColor = (node, colorScheme) => {
      switch (colorScheme) {
        case "energy":
          const energy = node.energy_weight || 0.5;
          return `hsl(${energy * 120}, 70%, 60%)`;
        case "emotion":
          const emotion = node.composite_emotion;
          if ((emotion == null ? void 0 : emotion.valence) !== void 0) {
            const hue = emotion.valence > 0 ? 120 : 0;
            const saturation = Math.abs(emotion.valence) * 70 + 30;
            return `hsl(${hue}, ${saturation}%, 60%)`;
          }
          return "#8c8c8c";
        case "age":
          if (node.created_at) {
            const age = (Date.now() - new Date(node.created_at).getTime()) / (1e3 * 60 * 60 * 24);
            const hue = Math.max(0, 240 - age * 10);
            return `hsl(${hue}, 70%, 60%)`;
          }
          return "#8c8c8c";
        case "tags":
          const tags = node.tags || [];
          if (tags.length > 0) {
            const hash = tags.join("").split("").reduce((a, b) => {
              a = (a << 5) - a + b.charCodeAt(0);
              return a & a;
            }, 0);
            const hue = Math.abs(hash) % 360;
            return `hsl(${hue}, 70%, 60%)`;
          }
          return "#8c8c8c";
        default:
          return "#1890ff";
      }
    };
    const drawTrendLines = (ctx, nodes) => {
      const nodesByTime = Object.values(nodes).filter((n) => n.created_at).sort((a, b) => new Date(a.created_at).getTime() - new Date(b.created_at).getTime());
      if (nodesByTime.length < 2) return;
      ctx.strokeStyle = "rgba(255, 165, 0, 0.3)";
      ctx.lineWidth = 2;
      ctx.setLineDash([5, 5]);
      ctx.beginPath();
      nodesByTime.forEach((node, i) => {
        if (i === 0) {
          ctx.moveTo(node.x, node.y);
        } else {
          ctx.lineTo(node.x, node.y);
        }
      });
      ctx.stroke();
      ctx.setLineDash([]);
    };
    const drawClusters = (ctx, nodes) => {
      const nodeList = Object.values(nodes);
      const clusters = [];
      const visited = /* @__PURE__ */ new Set();
      nodeList.forEach((node) => {
        if (visited.has(node.id)) return;
        const cluster = [node];
        visited.add(node.id);
        nodeList.forEach((other) => {
          if (visited.has(other.id)) return;
          const dx = node.x - other.x;
          const dy = node.y - other.y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < 100) {
            cluster.push(other);
            visited.add(other.id);
          }
        });
        if (cluster.length > 1) {
          clusters.push(cluster);
        }
      });
      clusters.forEach((cluster, i) => {
        const hue = i * 137.5 % 360;
        ctx.strokeStyle = `hsla(${hue}, 50%, 50%, 0.3)`;
        ctx.fillStyle = `hsla(${hue}, 50%, 50%, 0.1)`;
        ctx.lineWidth = 2;
        const xs = cluster.map((n) => n.x);
        const ys = cluster.map((n) => n.y);
        const minX = Math.min(...xs) - 20;
        const maxX = Math.max(...xs) + 20;
        const minY = Math.min(...ys) - 20;
        const maxY = Math.max(...ys) + 20;
        ctx.beginPath();
        ctx.roundRect(minX, minY, maxX - minX, maxY - minY, 10);
        ctx.fill();
        ctx.stroke();
      });
    };
    const handleMouseDown = (e) => {
      setIsDragging(true);
      setDragStart({ x: e.clientX - pan.x, y: e.clientY - pan.y });
    };
    const handleMouseMove = (e) => {
      var _a;
      if (isDragging && dragStart) {
        setPan({
          x: e.clientX - dragStart.x,
          y: e.clientY - dragStart.y
        });
      } else {
        const rect = (_a = canvasRef.current) == null ? void 0 : _a.getBoundingClientRect();
        if (!rect) return;
        const x = (e.clientX - rect.left - width / 2 - pan.x) / scale;
        const y = (e.clientY - rect.top - height / 2 - pan.y) / scale;
        let hoveredNodeId = null;
        for (const node of Object.values(layout.nodes)) {
          const dx = x - node.x;
          const dy = y - node.y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          const radius = 8 + (node.energy_weight || 0.5) * 12;
          if (dist <= radius) {
            hoveredNodeId = node.id;
            break;
          }
        }
        setHoveredNode(hoveredNodeId);
      }
    };
    const handleMouseUp = () => {
      setIsDragging(false);
      setDragStart(null);
    };
    const handleClick = (e) => {
      var _a;
      if (isDragging) return;
      const rect = (_a = canvasRef.current) == null ? void 0 : _a.getBoundingClientRect();
      if (!rect) return;
      const x = (e.clientX - rect.left - width / 2 - pan.x) / scale;
      const y = (e.clientY - rect.top - height / 2 - pan.y) / scale;
      for (const node of Object.values(layout.nodes)) {
        const dx = x - node.x;
        const dy = y - node.y;
        const dist = Math.sqrt(dx * dx + dy * dy);
        const radius = 8 + (node.energy_weight || 0.5) * 12;
        if (dist <= radius) {
          setSelectedNode(node.id);
          onNodeClick == null ? void 0 : onNodeClick(node);
          return;
        }
      }
      setSelectedNode(null);
    };
    const handleWheel = (e) => {
      const delta = e.deltaY > 0 ? 0.9 : 1.1;
      setScale((prev) => Math.max(0.1, Math.min(3, prev * delta)));
    };
    reactExports.useImperativeHandle(ref, () => ({
      exportPNG: () => {
        const canvas = canvasRef.current;
        return canvas ? canvas.toDataURL("image/png") : null;
      },
      exportSVG: () => {
        return null;
      },
      resetView: () => {
        setPan({ x: 0, y: 0 });
        setScale(1);
        setSelectedNode(null);
      },
      focusNode: (nodeId) => {
        const node = layout.nodes[nodeId];
        if (node) {
          setPan({ x: -node.x * scale, y: -node.y * scale });
          setSelectedNode(nodeId);
        }
      },
      getVisibleNodes: () => {
        return Object.values(layout.nodes);
      },
      applyFilter: (filter) => {
        if (!data) return;
        const filteredNodes = data.nodes.filter(filter);
        const filteredNodeIds = new Set(filteredNodes.map((n) => n.id));
        const filteredEdges = data.edges.filter(
          (edge) => filteredNodeIds.has(edge.source) && filteredNodeIds.has(edge.target)
        );
        setFilteredData({ nodes: filteredNodes, edges: filteredEdges });
      }
    }));
    const availableTags = Array.from(new Set(
      ((data == null ? void 0 : data.nodes) || []).flatMap((node) => node.tags || [])
    ));
    Array.from(new Set(
      ((data == null ? void 0 : data.nodes) || []).map((node) => node.state || "unknown")
    ));
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { position: "relative" }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Card,
        {
          size: "small",
          style: {
            position: "absolute",
            top: 10,
            left: 10,
            zIndex: 10,
            minWidth: 200
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", size: "small", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { wrap: true, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Tooltip, { title: "Увеличить", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  size: "small",
                  icon: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$1, { size: 14 }),
                  onClick: () => setScale((prev) => Math.min(3, prev * 1.2))
                }
              ) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Tooltip, { title: "Уменьшить", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  size: "small",
                  icon: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon, { size: 14 }),
                  onClick: () => setScale((prev) => Math.max(0.1, prev / 1.2))
                }
              ) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Tooltip, { title: "Сбросить вид", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  size: "small",
                  icon: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$5, { size: 14 }),
                  onClick: () => {
                    setPan({ x: 0, y: 0 });
                    setScale(1);
                  }
                }
              ) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Tooltip, { title: "Экспорт PNG", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  size: "small",
                  icon: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$4, { size: 14 }),
                  onClick: () => {
                    const canvas = canvasRef.current;
                    if (canvas) {
                      const link = document.createElement("a");
                      link.download = "soul-graph.png";
                      link.href = canvas.toDataURL();
                      link.click();
                    }
                  }
                }
              ) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Размер узлов:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Slider,
                {
                  min: 0.5,
                  max: 2,
                  step: 0.1,
                  value: visualSettings.nodeSize,
                  onChange: (value) => setVisualSettings((prev) => ({ ...prev, nodeSize: value })),
                  style: { width: 80 }
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Цветовая схема:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Select,
                {
                  size: "small",
                  value: visualSettings.colorScheme,
                  onChange: (value) => setVisualSettings((prev) => ({ ...prev, colorScheme: value })),
                  style: { width: 100 },
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Select.Option, { value: "energy", children: "Энергия" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Select.Option, { value: "emotion", children: "Эмоции" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Select.Option, { value: "age", children: "Возраст" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Select.Option, { value: "tags", children: "Теги" })
                  ]
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { wrap: true, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Switch,
                {
                  size: "small",
                  checked: showHeatmap,
                  onChange: (checked) => setVisualSettings((prev) => ({ ...prev, showHeatmap: checked }))
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Тепловая карта" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Switch,
                {
                  size: "small",
                  checked: visualSettings.showLabels,
                  onChange: (checked) => setVisualSettings((prev) => ({ ...prev, showLabels: checked }))
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Метки" })
            ] })
          ] })
        }
      ),
      enableFiltering && /* @__PURE__ */ jsxRuntimeExports.jsx(
        Card,
        {
          size: "small",
          style: {
            position: "absolute",
            top: 10,
            right: 10,
            zIndex: 10,
            minWidth: 250
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", size: "small", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Энергия:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Slider,
                {
                  range: true,
                  min: 0,
                  max: 1,
                  step: 0.01,
                  value: [filters.minEnergy, filters.maxEnergy],
                  onChange: ([min, max]) => setFilters((prev) => ({ ...prev, minEnergy: min, maxEnergy: max }))
                }
              )
            ] }),
            availableTags.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Теги:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Select,
                {
                  mode: "multiple",
                  size: "small",
                  placeholder: "Выберите теги",
                  value: filters.selectedTags,
                  onChange: (tags) => setFilters((prev) => ({ ...prev, selectedTags: tags })),
                  style: { width: "100%" },
                  children: availableTags.map((tag) => /* @__PURE__ */ jsxRuntimeExports.jsx(Select.Option, { value: tag, children: tag }, tag))
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Период (дни назад):" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Slider,
                {
                  range: true,
                  min: 0,
                  max: 90,
                  value: filters.timeRange,
                  onChange: (range) => setFilters((prev) => ({ ...prev, timeRange: range }))
                }
              )
            ] })
          ] })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "canvas",
        {
          ref: canvasRef,
          style: {
            border: "1px solid #d9d9d9",
            cursor: isDragging ? "grabbing" : "grab",
            // Предотвращаем нежелательный скролл/зум страницы на тач/колесо
            touchAction: "none",
            overscrollBehavior: "none"
          },
          onMouseDown: handleMouseDown,
          onMouseMove: handleMouseMove,
          onMouseUp: handleMouseUp,
          onMouseLeave: handleMouseUp,
          onClick: handleClick,
          onWheel: handleWheel
        }
      ),
      selectedNode && layout.nodes[selectedNode] && /* @__PURE__ */ jsxRuntimeExports.jsx(
        Card,
        {
          size: "small",
          title: "Информация о узле",
          style: {
            position: "absolute",
            bottom: 10,
            left: 10,
            zIndex: 10,
            maxWidth: 300
          },
          children: (() => {
            const node = layout.nodes[selectedNode];
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", size: "small", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "ID:" }),
                " ",
                node.id
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Энергия:" }),
                " ",
                (node.energy_weight || 0).toFixed(3)
              ] }),
              node.tags && node.tags.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Теги:" }),
                " ",
                node.tags.join(", ")
              ] }),
              node.thought_form && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Мысль:" }),
                " ",
                node.thought_form.substring(0, 100),
                "..."
              ] }),
              node.composite_emotion && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Эмоция:" }),
                " ",
                node.composite_emotion.label,
                "(валентность: ",
                (node.composite_emotion.valence || 0).toFixed(2),
                ")"
              ] })
            ] });
          })()
        }
      )
    ] });
  }
);
const { Title, Text } = Typography;
const { Search } = Input;
function SoulVisualization() {
  const [loading, setLoading] = reactExports.useState(false);
  const [graphData, setGraphData] = reactExports.useState(null);
  const [metrics, setMetrics] = reactExports.useState(null);
  const [selectedNode, setSelectedNode] = reactExports.useState(null);
  const [neighbors, setNeighbors] = reactExports.useState([]);
  const [neighborsLoading, setNeighborsLoading] = reactExports.useState(false);
  const [searchQuery, setSearchQuery] = reactExports.useState("");
  const [settings, setSettings] = reactExports.useState({
    depth: 2,
    min_energy: 0.1,
    max_nodes: 100,
    include_archived: false,
    connection_threshold: 0.2
  });
  const graphRef = reactExports.useRef(null);
  const loadGraphData = async (centerNodeId) => {
    try {
      setLoading(true);
      const params = new URLSearchParams({
        depth: settings.depth.toString(),
        min_energy: settings.min_energy.toString(),
        max_nodes: settings.max_nodes.toString(),
        include_archived: settings.include_archived.toString(),
        connection_threshold: settings.connection_threshold.toString()
      });
      if (centerNodeId) {
        params.append("center_id", centerNodeId);
      }
      let data = await apiRequest(`/admin/soul/graph/visual_subgraph?${params}`, "GET");
      if ((!data.nodes || data.nodes.length === 0) && (!data.edges || data.edges.length === 0)) {
        data = await apiRequest(`/admin/soul/graph/visual_overview?limit_nodes=${settings.max_nodes}&min_strength=${settings.connection_threshold}`, "GET");
      }
      if ((!data.nodes || data.nodes.length === 0) && (!data.edges || data.edges.length === 0)) {
        const hubsResp = await apiRequest(`/admin/soul/graph/hubs?min_strength=0&limit=${Math.min(200, Math.max(20, settings.max_nodes))}`, "GET");
        const edgesResp = await apiRequest(`/admin/soul/graph/strong_edges?min_strength=0&limit=${Math.min(2e3, settings.max_nodes * 10)}`, "GET");
        const hubNodes = (hubsResp.items || []).map((h) => ({
          id: String(h.quant_id),
          energy_weight: Number(h.energy_weight || 0),
          thought_form: String(h.thought_form || "")
        }));
        const allowed = new Set(hubNodes.map((n) => n.id));
        const allEdges = (edgesResp.items || []).filter((e) => allowed.has(String(e.source)) && allowed.has(String(e.target)));
        data = { nodes: hubNodes, edges: allEdges };
      }
      const graphData2 = {
        nodes: data.nodes || [],
        edges: data.edges || []
      };
      setGraphData(graphData2);
      const nodeCount = graphData2.nodes.length;
      const metrics2 = {
        total_nodes: nodeCount,
        total_edges: graphData2.edges.length,
        avg_energy: nodeCount > 0 ? graphData2.nodes.reduce((sum, node) => sum + (node.energy_weight || 0), 0) / nodeCount : 0,
        max_energy: nodeCount > 0 ? Math.max(...graphData2.nodes.map((node) => node.energy_weight || 0)) : 0,
        clusters_count: estimateClusters(graphData2),
        connectivity: graphData2.edges.length / Math.max(1, nodeCount)
      };
      setMetrics(metrics2);
      staticMethods.success(`Загружен граф: ${metrics2.total_nodes} узлов, ${metrics2.total_edges} связей`);
    } catch (error) {
      console.error("Ошибка загрузки графа:", error);
      staticMethods.error("Ошибка загрузки данных графа");
    } finally {
      setLoading(false);
    }
  };
  const loadNeighbors = async (nodeId) => {
    try {
      setNeighborsLoading(true);
      const resp = await apiRequest(`/admin/soul/graph/top_neighbors?quant_id=${encodeURIComponent(nodeId)}&limit=50`, "GET");
      setNeighbors(Array.isArray(resp.items) ? resp.items : []);
    } catch (e) {
      setNeighbors([]);
    } finally {
      setNeighborsLoading(false);
    }
  };
  const searchNodes = async (query) => {
    if (!query.trim()) {
      await loadGraphData();
      return;
    }
    try {
      setLoading(true);
      const response = await fetch(`/api/admin/soul/search/nodes?q=${encodeURIComponent(query)}&limit=50`, {
        headers: buildAuthHeaders()
      });
      if (response.ok) {
        const data = await response.json();
        const foundNodes = data.nodes || [];
        if (foundNodes.length > 0) {
          const centerNodeId = foundNodes[0].id;
          await loadGraphData(centerNodeId);
          if (graphRef.current) {
            graphRef.current.focusNode(centerNodeId);
          }
          staticMethods.success(`Найдено узлов: ${foundNodes.length}`);
        } else {
          staticMethods.info("Узлы не найдены");
        }
      }
    } catch (error) {
      console.error("Ошибка поиска:", error);
      staticMethods.error("Ошибка поиска узлов");
    } finally {
      setLoading(false);
    }
  };
  const estimateClusters = (data) => {
    const nodeIds = new Set(data.nodes.map((n) => n.id));
    const visited = /* @__PURE__ */ new Set();
    let clusters = 0;
    for (const node of data.nodes) {
      if (visited.has(node.id)) continue;
      const queue = [node.id];
      visited.add(node.id);
      while (queue.length > 0) {
        const currentId = queue.shift();
        for (const edge of data.edges) {
          let connectedId = null;
          if (edge.source === currentId && !visited.has(edge.target)) {
            connectedId = edge.target;
          } else if (edge.target === currentId && !visited.has(edge.source)) {
            connectedId = edge.source;
          }
          if (connectedId && nodeIds.has(connectedId)) {
            visited.add(connectedId);
            queue.push(connectedId);
          }
        }
      }
      clusters++;
    }
    return clusters;
  };
  const handleNodeClick = (node) => {
    setSelectedNode(node);
    if (node == null ? void 0 : node.id) loadNeighbors(node.id);
  };
  const unlinkEdge = async (fromId, toId) => {
    try {
      const reason = window.prompt("Причина разрыва связи:", "") || "";
      const body = { from_quant_id: fromId, to_quant_id: toId, bidirectional: true, reason };
      await apiRequest("/admin/soul/graph/unlink_edge", "POST", body);
      staticMethods.success("Связь разорвана");
      setGraphData((prev) => prev ? {
        nodes: prev.nodes,
        edges: prev.edges.filter((e) => !(e.source === fromId && e.target === toId || e.source === toId && e.target === fromId))
      } : prev);
      if (selectedNode == null ? void 0 : selectedNode.id) loadNeighbors(selectedNode.id);
    } catch (e) {
      staticMethods.error("Не удалось разорвать связь");
    }
  };
  const restoreEdge = async (fromId) => {
    try {
      const toId = window.prompt("ID второго узла (to_quant_id):", "") || "";
      if (!toId) return;
      const type = window.prompt("Тип связи (semantic|causal|episodic|goal_link):", "semantic") || "semantic";
      const strengthStr = window.prompt("Сила связи (0..1):", "0.5") || "0.5";
      const overlapStr = window.prompt("Пересечение ключевых слов (0..1):", "0.0") || "0.0";
      const reason = window.prompt("Причина восстановления:", "") || "";
      const body = {
        from_quant_id: fromId,
        to_quant_id: toId,
        connection_type: type,
        connection_strength: Math.max(0, Math.min(1, parseFloat(strengthStr))),
        keyword_overlap: Math.max(0, Math.min(1, parseFloat(overlapStr))),
        bidirectional: false,
        reason
      };
      await apiRequest("/admin/soul/graph/restore_edge", "POST", body);
      staticMethods.success("Связь восстановлена");
      setGraphData((prev) => prev ? {
        nodes: prev.nodes,
        edges: [...prev.edges, { source: fromId, target: toId, strength: body.connection_strength, connection_type: type, keyword_overlap: body.keyword_overlap }]
      } : prev);
      if (selectedNode == null ? void 0 : selectedNode.id) loadNeighbors(selectedNode.id);
    } catch (e) {
      staticMethods.error("Не удалось восстановить связь");
    }
  };
  const exportGraph = (format) => {
    if (format === "png" && graphRef.current) {
      const dataUrl = graphRef.current.exportPNG();
      if (dataUrl) {
        const link = document.createElement("a");
        link.download = `soul-graph-${Date.now()}.png`;
        link.href = dataUrl;
        link.click();
        staticMethods.success("Граф экспортирован как PNG");
      }
    } else if (format === "json" && graphData) {
      const dataStr = JSON.stringify(graphData, null, 2);
      const dataBlob = new Blob([dataStr], { type: "application/json" });
      const url = URL.createObjectURL(dataBlob);
      const link = document.createElement("a");
      link.download = `soul-graph-data-${Date.now()}.json`;
      link.href = url;
      link.click();
      URL.revokeObjectURL(url);
      staticMethods.success("Данные графа экспортированы как JSON");
    }
  };
  reactExports.useEffect(() => {
    loadGraphData();
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { padding: "var(--sp-spacing-sm)", boxSizing: "border-box", maxWidth: "100%" }, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginBottom: "var(--sp-spacing-sm)" }, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Title, { level: 2, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$6, { style: { marginRight: 8, verticalAlign: "middle" } }),
        "Визуализация Soul"
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { type: "secondary", children: "Интерактивная визуализация графа Квантов сознания Soul" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Row, { gutter: [16, 16], children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { xs: 24, lg: 6, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", style: { width: "100%" }, size: "middle", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { title: "Поиск", size: "small", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Search,
          {
            placeholder: "Поиск по содержимому Квантов...",
            value: searchQuery,
            onChange: (e) => setSearchQuery(e.target.value),
            onSearch: searchNodes,
            loading,
            enterButton: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$2, { size: 16 })
          }
        ) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { title: "Настройки", size: "small", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", style: { width: "100%" }, size: "small", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { children: [
              "Глубина связей: ",
              settings.depth
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Slider,
              {
                min: 1,
                max: 5,
                value: settings.depth,
                onChange: (value) => setSettings((prev) => ({ ...prev, depth: value }))
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { children: [
              "Мин. энергия: ",
              settings.min_energy
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Slider,
              {
                min: 0,
                max: 1,
                step: 0.01,
                value: settings.min_energy,
                onChange: (value) => setSettings((prev) => ({ ...prev, min_energy: value }))
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { children: [
              "Макс. узлов: ",
              settings.max_nodes
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Slider,
              {
                min: 10,
                max: 500,
                value: settings.max_nodes,
                onChange: (value) => setSettings((prev) => ({ ...prev, max_nodes: value }))
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { children: [
              "Порог связей: ",
              settings.connection_threshold
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Slider,
              {
                min: 0,
                max: 1,
                step: 0.01,
                value: settings.connection_threshold,
                onChange: (value) => setSettings((prev) => ({ ...prev, connection_threshold: value }))
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Switch,
              {
                checked: settings.include_archived,
                onChange: (checked) => setSettings((prev) => ({ ...prev, include_archived: checked }))
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { style: { marginLeft: 8 }, children: "Включить архивные" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              type: "primary",
              block: true,
              onClick: () => loadGraphData(),
              loading,
              children: "Применить настройки"
            }
          )
        ] }) }),
        metrics && /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { title: "Метрики графа", size: "small", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Row, { gutter: [8, 8], children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Statistic,
            {
              title: "Узлы",
              value: metrics.total_nodes,
              prefix: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$7, { size: 16 })
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Statistic,
            {
              title: "Связи",
              value: metrics.total_edges,
              prefix: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$6, { size: 16 })
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Statistic,
            {
              title: "Ср. энергия",
              value: metrics.avg_energy,
              precision: 3,
              prefix: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$8, { size: 16 })
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Statistic,
            {
              title: "Кластеры",
              value: metrics.clusters_count,
              prefix: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$3, { size: 16 })
            }
          ) })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { title: "Экспорт", size: "small", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", style: { width: "100%" }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              block: true,
              icon: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$4, { size: 16 }),
              onClick: () => exportGraph("png"),
              children: "Экспорт PNG"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              block: true,
              icon: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$4, { size: 16 }),
              onClick: () => exportGraph("json"),
              children: "Экспорт JSON"
            }
          )
        ] }) }),
        selectedNode && /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { title: "Связи узла", size: "small", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", style: { width: "100%" }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              type: "dashed",
              onClick: () => restoreEdge(selectedNode.id),
              disabled: !(selectedNode == null ? void 0 : selectedNode.id),
              children: "Создать/восстановить связь…"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { maxHeight: 240, overflow: "auto", border: "1px solid var(--sp-border-color, var(--border-color))", padding: 8, borderRadius: 6 }, children: neighborsLoading ? /* @__PURE__ */ jsxRuntimeExports.jsx(Spin, {}) : neighbors.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { type: "secondary", children: "Соседи не найдены" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Space, { direction: "vertical", style: { width: "100%" }, size: 8, children: neighbors.map((n) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { display: "flex", gap: 8, alignItems: "center", justifyContent: "space-between" }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { strong: true, children: "ID:" }),
                " ",
                n.neighbor_id
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { fontSize: 12 }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { type: "secondary", children: [
                "strength: ",
                (n.strength || 0).toFixed(3),
                " · overlap: ",
                (n.keyword_overlap || 0).toFixed(3)
              ] }) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "small", danger: true, onClick: () => unlinkEdge(selectedNode.id, n.neighbor_id), children: "Разорвать" })
          ] }, n.neighbor_id)) }) })
        ] }) })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { xs: 24, lg: 18, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(Spin, { spinning: loading, children: graphData ? /* @__PURE__ */ jsxRuntimeExports.jsx(
        EnhancedGraphCanvas,
        {
          ref: graphRef,
          data: graphData,
          width: 800,
          height: 600,
          onNodeClick: handleNodeClick,
          showHeatmap: false,
          showClusters: true,
          showTrends: true,
          enableFiltering: true
        }
      ) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { height: 600, display: "flex", alignItems: "center", justifyContent: "center", background: "var(--sp-surface-muted, #fafafa)", border: "2px dashed var(--sp-border-color, #d9d9d9)", borderRadius: 8 }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Space, { direction: "vertical", align: "center", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$6, { size: 48, style: { color: "var(--sp-icon-muted, #bfbfbf)" } }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { type: "secondary", children: "Граф не загружен" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "primary", onClick: () => loadGraphData(), children: "Загрузить граф" })
      ] }) }) }) }) })
    ] }),
    selectedNode && /* @__PURE__ */ jsxRuntimeExports.jsx(
      Card,
      {
        title: "Детали узла",
        style: { marginTop: "var(--sp-spacing-sm)" },
        extra: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            size: "small",
            onClick: () => {
              setSelectedNode(null);
              setNeighbors([]);
            },
            children: "Закрыть"
          }
        ),
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Tabs, { defaultActiveKey: "general", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tabs.TabPane, { tab: "Общее", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Row, { gutter: [16, 16], children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Statistic,
              {
                title: "ID",
                value: selectedNode.id,
                valueStyle: { fontSize: 14 }
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Statistic,
              {
                title: "Энергия",
                value: selectedNode.energy_weight || 0,
                precision: 4,
                prefix: /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$8, { size: 16 })
              }
            ) }),
            selectedNode.delta_energy && /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Statistic,
              {
                title: "Изменение энергии",
                value: selectedNode.delta_energy,
                precision: 4,
                valueStyle: {
                  color: selectedNode.delta_energy > 0 ? "#52c41a" : "#f5222d"
                },
                prefix: selectedNode.delta_energy > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$9, { size: 16 }) : /* @__PURE__ */ jsxRuntimeExports.jsx(RefIcon$9, { size: 16, style: { transform: "rotate(180deg)" } })
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 12, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Statistic,
              {
                title: "Состояние",
                value: selectedNode.state || "unknown"
              }
            ) })
          ] }) }, "general"),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Tabs.TabPane, { tab: "Содержание", children: [
            selectedNode.thought_form && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginBottom: 16 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { strong: true, children: "Форма мысли:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { marginTop: 8, padding: 12, background: "var(--sp-surface-muted, #f5f5f5)", borderRadius: 6, whiteSpace: "pre-wrap" }, children: selectedNode.thought_form })
            ] }),
            selectedNode.tags && selectedNode.tags.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { marginBottom: 16 }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { strong: true, children: "Теги:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { style: { marginTop: 8 }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Space, { wrap: true, children: selectedNode.tags.map((tag, index) => /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "small", type: "default", children: tag }, index)) }) })
            ] })
          ] }, "content"),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tabs.TabPane, { tab: "Эмоции", children: selectedNode.composite_emotion ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Row, { gutter: [16, 16], children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 8, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Statistic,
              {
                title: "Эмоция",
                value: selectedNode.composite_emotion.label || "Неопределена"
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 8, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Statistic,
              {
                title: "Валентность",
                value: selectedNode.composite_emotion.valence || 0,
                precision: 3,
                valueStyle: {
                  color: (selectedNode.composite_emotion.valence || 0) > 0 ? "#52c41a" : "#f5222d"
                }
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Col, { span: 8, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              Statistic,
              {
                title: "Интенсивность",
                value: selectedNode.composite_emotion.intensity || 0,
                precision: 3
              }
            ) })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { message: "Эмоциональные данные отсутствуют", type: "info" }) }, "emotions")
        ] })
      }
    )
  ] });
}
export {
  SoulVisualization as default
};
//# sourceMappingURL=SoulVisualization-B8QEjFCZ.js.map
