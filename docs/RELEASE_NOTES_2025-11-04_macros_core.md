# Release 2025-11-04 (macros core)

- Macros router added and enabled (`/api/macros/*`).
- Signed-first path + robust admin/exec wrapper (`admin_exec_post.py`).
- Key Master loopback stabilized; secrets health OK.
- Inspectors RUN_ALL green; smokes green.
- Deployment orchestration updated (`run_triz_deploy.ps1`, depth fixes for JSON).


